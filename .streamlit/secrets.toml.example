# Local only: copy to ".streamlit/secrets.toml" (gitignored).
# Streamlit Community Cloud: paste the same key/value in App settings → Secrets.

SMARTTICKET_API_BASE_URL = "https://your-fastapi-host.example.com"
