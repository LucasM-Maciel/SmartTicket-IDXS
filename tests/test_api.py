"""API tests — to be implemented when FastAPI routes exist.

Cover ``POST /predict`` and ``POST /health`` using ``TestClient`` or ``httpx``,
per ``docs/api-contracts.md`` and ``docs/project-context.md``.
"""
